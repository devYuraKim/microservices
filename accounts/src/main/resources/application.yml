# Server
server:
  port: 8080

# Spring
spring:
  application:
    name: "accounts"
  profiles:
    active: "prod"
  config:
    import: "optional:configserver:http://localhost:8071/" #config가 없어도 service 실행 가능
  rabbitmq: #docker
    host: localhost
    port: 5672
    username: guest
    password: guest
  datasource:
    url: jdbc:mysql://localhost:3306/accountsdb
    username: root
    password: root
  sql:
    init:
      mode: always
  jpa:
    show-sql: true
  output:
    ansi:
      enabled: always

management:
  endpoints:
    web:
      exposure:
        include: "*"

# Logging TRACE < DEBUG < INFO < WARN < ERROR
logging:
  level:
    root: INFO